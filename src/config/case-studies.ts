import type { CaseStudy } from "@/types";

/**
 * Case studies data.
 *
 * IMPORTANT: Per spec Section 4.4, do NOT populate with fabricated results.
 * Leave this array empty until at least one real case study exists.
 * The /results page will show a "coming soon" state when this array is empty.
 */
export const caseStudies: CaseStudy[] = [];
